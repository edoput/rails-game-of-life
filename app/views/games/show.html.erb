<nav>
  <%= link_to "All games", root_path %>
</nav>

<h2><%= @game.name %></h2>

<% if @game.started? %>
<h3>Generations</h3>

There are <%= @game.generations.count %> generations available.

<p>Game starts <%= link_to "here", game_generation_path(@game, @initial_generation) %>.</p>
<p>Last seen <%= link_to "here", game_generation_path(@game, @current_generation) %>.</p>
								
<!-- TODO(edoput) show paginated list of generations -->
<% else %>
  <p>Upload a file to start the game</p>
  <%= form_with model: [@game, @game.generations.build ] do |form| %>
    <%= form.label :file %><br>
    <%= form.file_field :initial_board %>
    <%= form.submit "Start game" %>
  <% end %>
<% end %>

